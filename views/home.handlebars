<section>
    {{#each data}}
    <section>
        <article>
            <div>
                <h1>{{post_title}}</h1>
                <p></p>
            </div>
            <p>{{post_message}}</p>
        </article>
        <section>
        {{#each comment}}
            <div>
                <h3>{{user.name}}</h3>
                <p>{{comment}}</p>
                <p>{{time}}</p>
            </div>
        {{/each}}
        </section>
        {{#if ../loggedIn}}
        <input>
        <button class="addCommentButton">Add comment</button>
        {{/if}}
    </section>
    {{/each}}
</section>