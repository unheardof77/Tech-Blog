<section>
    {{#each data}}
        <article>
            <div>
                <h1>{{post_title}}</h1>
                <p></p>
            </div>
            <p>{{post_message}}</p>
        </article>
    {{/each}}
</section>